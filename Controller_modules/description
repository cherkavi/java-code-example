Данный пакет предназначен для упорядочивания общения между модулями,
которые находятся в связке и являются "клиентами" для одного "сервера"
все модули подключены к одной "магистрали" через 485 интерфейс.

модуль-сервер отправляет запрос, а модуль клиент обрабатывает этот запрос
(модули могут отвечать на запросы, а могут и не отвечать, а просто отрабатывать действия...)
передачу может инициировать только модуль-сервер

в классы данного пакета являются командами для модулей

Реализованные команды имеют соответствующее название в классах (без префикса из спец-символа)

 
