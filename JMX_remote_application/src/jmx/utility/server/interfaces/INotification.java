package jmx.utility.server.interfaces;

/** marker-interface for mark all methods in object as send notification (setter objects ) <br />
 * <b>!!!important</b> need to extend interface and than make a realization of inherited interface
 * */
public interface INotification {
}
